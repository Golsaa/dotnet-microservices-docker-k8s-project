apiVersion: apps/v1
kind: Deployment

metadata:
  name: commands-mssql-depl

spec:
  replicas: 1

  selector:
    matchLabels:
      app: commands-mssql

  template:
    metadata:
      labels:
        app: commands-mssql

    spec:
      containers:
        - name: commands-mssql
          image: mcr.microsoft.com/mssql/server:2022-latest

          ports:
            - containerPort: 1433

          env:
            - name: ACCEPT_EULA
              value: "Y"

            - name: MSSQL_SA_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: commands-mssql-secret
                  key: SA_PASSWORD

          volumeMounts:
            - name: commands-mssql-storage
              mountPath: /var/opt/mssql

      volumes:
        - name: commands-mssql-storage
          persistentVolumeClaim:
            claimName: commands-mssql-claim

---
apiVersion: v1
kind: Service

metadata:
  name: commands-mssql-clusterip-srv

spec:
  type: ClusterIP

  selector:
    app: commands-mssql

  ports:
    - name: mssql
      protocol: TCP
      port: 1433
      targetPort: 1433